# 隐私说明

[English](PRIVACY.md) | **简体中文**

Add2Calendar 在你的浏览器中运行。开发者不运营后端，不接收你的数据，也不使用数据统计或遥测服务。

## 访问 PDC

扩展使用当前 PDC 标签页已有的登录令牌，读取你的报名记录和活动时间安排。请求仅发送到 PDC 网站本身。扩展不会持久保存这个令牌，不会将它传递给扩展预览页面、写入日历文件或发送给日历服务商。

扩展不会读取或自动操作你的登录密码、双重认证码，也不会改变报名或退选状态。

传递到扩展预览的内容仅包含活动字段：名称、活动编码、时间、地点、讲者、备注、咨询邮箱以及固定的 PDC 来源链接。学生个人资料字段不会被复制。预览草稿保存在浏览器会话存储中，24 小时后失效；创建新草稿时会清理过期草稿。浏览器会话结束后，会话存储会被清空。

## 访问 SIS

在 **My Class Schedule（我的课表）**页面中，SIS 会通过自身同源接口加载当前登录学生的课表。Add2Calendar 只观察这个响应，不会修改它；它仅保留课程编号、名称、班别、选课状态、日期、星期、时间、教室和教师姓名。你点击 **Add class schedule** 后，这些字段才会传递给扩展后台。课表进入扩展预览前，学号和认证数据已经在页面中被丢弃。

候补和已退课程不会被导出。扩展不会在 SIS 中提交选课、退课、换课或审批请求，也不会读取密码、认证码、成绩、学分、截止日期或其他无关页面。

## 日历数据的去向

- **Outlook / Google：** 打开某个场次时，扩展通过预填日历链接将该场次详情发送给所选服务商，由你在其页面完成保存。链接可能保留在浏览器历史记录中，相应服务商的账号和隐私政策适用。
- **Nextcloud：** 点击添加后，选中的活动详情以及包含认证信息的请求头会通过 HTTPS 发送到你配置的具体日历地址。扩展拒绝重定向，不会发送会议邀请或参会人邮件。
- **日历文件：** 扩展在本地生成 `.ics` 下载文件。是否导入或分享文件，由你决定。批量导出同样在本地完成，之后由你将文件导入所选日历。

## Outlook 清理（可选）

Outlook 清理默认关闭。要启用它，你必须打开扩展设置，填写 Microsoft Entra 应用程序客户端 ID，点击**启用 Outlook 清理**，并允许 Chrome 访问 Microsoft 登录和 Graph。打开清理工具后仍不会自动连接或读取数据：你还需要点击**连接**，在 Microsoft 页面同意委托的 `Calendars.ReadWrite` 权限，选择日历和日期范围，再点击**加载日程**。

Microsoft 访问令牌只保存在清理标签页的内存中；令牌过期、你主动断开或关闭标签页时即被丢弃。令牌不会写入扩展存储，也不会发送给开发者。本地只保存启用状态和公开的应用程序客户端 ID。停用功能时，这些设置及可选的 Microsoft 主机访问权限都会被移除。

对于你选择的日历和日期范围，Microsoft Graph 会返回日程 ID、标题、时间、地点、组织者、周期类型、敏感级别和参会人记录。Add2Calendar 会立即丢弃参会人身份，只保留用于会议风险提示的参会人数。工具不会自动选择任何日程。删除前会再次展示已选内容，提示由你组织的会议和周期性条目，并要求勾选确认。只有已选日程 ID 会发回 Microsoft Graph 执行删除。删除由你组织的会议时，Outlook 可能会向参会者发送取消通知。

## 本地设置和权限

日历偏好、提醒选项、可选的 Nextcloud 连接信息及 Outlook 清理配置保存在 `chrome.storage.local`，不进入 Chrome 同步。Nextcloud 应用密码没有额外的静态加密保护。本地存储仅向受信任的扩展上下文开放，PDC 和 SIS 内容脚本无法访问。

扩展只会请求访问你填写的 HTTPS Nextcloud 服务器，或由你明确启用的 Microsoft 清理服务。清单中声明可选的 HTTPS 地址范围，是为了支持这些由用户选择的连接；安装时不会一次性申请访问所有地址。

在设置页点击 **Forget connection（忘记连接）**，可删除本地 Nextcloud 配置并撤销对应服务器的可选访问权限。在 Nextcloud 的安全设置中撤销应用密码，可使它在服务器端失效。

卸载扩展会删除其本地设置，但不会删除已经添加到日历的条目或已经下载的文件。
