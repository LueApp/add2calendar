{
  "name": "add2calendar",
  "version": "0.3.3",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node scripts/browser-test.mjs",
    "package": "python3 scripts/package.py",
    "site:build": "npm run package && node scripts/build-site.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "wrangler": "^4.129.0"
  }
}
